// # Binary Search
//
// Given a sorted array, write a function \`contains\` that returns true if the array contains the item and false if it doesn't. It should use the binary search algorithm.
//
// contains(array, item) => Boolean
//
// contains([1,2,3], 3) => true
//
// contains(['a','b','c','d','e'], 'b') => true
//
// contains([1,2,3], 5) => false

function contains(array, item){
  // your code here
  return
}

module.exports = contains;
