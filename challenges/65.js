// Given 3 int values, a b c, return their sum. However, if one of the values is 13 then it does not count towards the sum and all values to its right do not count. So for example, if b is 13, then both b and c do not count.
//
// lucky_sum(1, 2, 3) → 6
//
// lucky_sum(1, 2, 13) → 3
//
// lucky_sum(1, 13, 3) → 1
//
// lucky_sum(3, 1, 8) → 12
//
// lucky_sum(3, 1, 9) → 13
//
// lucky_sum(3, 2, 10) → 15
//
// lucky_sum(13, 13, 2) → 0
//
// lucky_sum(13, 85835, 295928592) → 0

function lucky_sum(a, b, c){
  // Add functionality here!
  return
}

module.exports = lucky_sum;
