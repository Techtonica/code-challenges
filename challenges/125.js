// Create a function called `makeRepeater` that takes a number parameter and returns a function.
//
// The returned function should take a string as a parameter and repeat the input string the number of times specified.
//
// ```
// const doubler = makeRepeater(2);
// ```
//
// ```
// const tripler = makeRepeater(3);
// ```
//
// ```
// doubler("a")
// ```
//
// ```
// // => "aa"
// ```
//
// ```
// tripler("b")
// ```
//
// ```
// // => "bbb"
// ```


function makeRepeater(times) {
  // return a function that takes in one string parameter and
  //    returns the inputted string repeated 'times' amount of times
}

module.exports = makeRepeater;
